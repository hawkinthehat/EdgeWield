# AGENTS

## Cursor Cloud specific instructions

This repository ("EdgeWield") is currently an empty project scaffold containing only a `README.md`.

### Current state
- No source code, frameworks, build tools, or dependencies are configured.
- No services to start, no tests to run, no linting configured.
- The update script is a no-op (`echo`) since there are no dependencies to install.

### When source code is added
Once the project is bootstrapped with actual code and dependencies, update this section and the VM update script accordingly. Key things to configure:
- Dependency installation (e.g. `npm install`, `pip install -r requirements.txt`, etc.)
- Lint, test, and build commands
- Service startup instructions and any non-obvious caveats
