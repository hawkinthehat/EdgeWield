{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "bash -lc 'set -a; [ -f .env ] && . ./.env; set +a; concurrently -n api,web -c magenta,cyan \"PORT=${API_PORT:-4000} pnpm --filter @workspace/api-server dev\" \"PORT=${WEB_PORT:-5173} BASE_PATH=${BASE_PATH:-/} VITE_API_PROXY_TARGET=${VITE_API_PROXY_TARGET:-http://localhost:${API_PORT:-4000}} pnpm --filter @workspace/web dev\"'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
